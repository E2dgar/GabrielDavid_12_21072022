import './index.css';

/**
 * Component for showing a custom legend
 */
const CustomLegendChart = ({ children }) => {
    return <ul className="custom-legend">{children}</ul>;
};

export default CustomLegendChart;
